name: reviewdog
on: [pull_request]
jobs:
  misspell:
    name: runner / misspell
    permissions:
      pull-requests: read
      issues: read
      repository-projects: read
    runs-on: ubuntu-latest
    steps:
      - name: Check out code.
        uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: misspell
        uses: reviewdog/action-misspell@1d5fb16725ffd175be67e0b52a7d643353656040
        with:
          github_token: ${{ secrets.github_token }}
          locale: "US"
          pattern: "*.md"
